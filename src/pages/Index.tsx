import { Layout } from '@/components/Layout';
import { TodoApp } from '@/components/TodoApp';

const Index = () => {
  return (
    <Layout>
      <TodoApp />
    </Layout>
  );
};

export default Index;